import { styled } from "styled-components";

export const StreamContainer = styled.div`
    .loading_container {
        width: 100%;
        aspect-ratio: 16/9;
        background-color: black;

        .loading {
            width: 80px;
        }
    }
`